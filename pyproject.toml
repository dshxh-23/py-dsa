[project]
name = "py_dsa"
version = "0.1.0"
description = "Add your description here"
readme = "docs/README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.3",
]

[tool.setuptools.packages.find]
include = ["core*", "problems*", "algorithms*", "structures*"]
